package com.it.order.enums;

public enum TypeDingdanEnum {

    one(1),
    cityAdd(2),
    CITYADD(3),
    TYPE1(4),
    ;

    private int id;

    private TypeDingdanEnum(int id) {
        this.id = id;
    }
}
